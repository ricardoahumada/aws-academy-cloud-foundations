# Kubernetes y EKS - Conceptos Básicos

> **Tiempo de lectura:** ~10 min | **Referencia:** [Amazon EKS Documentation](https://docs.aws.amazon.com/eks/latest/userguide/what-is-eks.html)

---

## Conceptos Fundamentales

### Kubernetes vs ECS

```
┌─────────────────────────────────────────────────────────────┐
│                      Comparativa                            │
│                                                             │
│   ECS                          Kubernetes (EKS)            │
│   ├── Task Definition    ───►    Pod                        │
│   ├── Service           ───►    Deployment                  │
│   ├── Cluster           ───►    Cluster                     │
│   ├── Task Role         ───►    ServiceAccount + RBAC       │
│   └── Auto Scaling      ───►    HPA / VPA / KEDA           │
└─────────────────────────────────────────────────────────────┘
```

### Arquitectura EKS

```
┌─────────────────────────────────────────────────────────────┐
│                    Amazon EKS                               │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              EKS Managed Plane                         │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │  │
│  │  │ API Server  │  │ etcd        │  │ Scheduler   │   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘   │  │
│  └──────────────────────────────────────────────────────┘  │
│                            │                                │
│           ┌────────────────┼────────────────┐             │
│           │                │                │             │
│  ┌────────┴───┐    ┌────────┴───┐    ┌────────┴───┐       │
│  │ Node Group │    │ Node Group │    │ Fargate    │       │
│  │ (EC2)      │    │ (EC2)      │    │ Profile    │       │
│  └────────────┘    └────────────┘    └────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

---

## Componentes Principales

### Pod

Unidad más pequeña de Kubernetes:

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
  labels:
    app: my-app
spec:
  containers:
  - name: webapp
    image: 123456789.dkr.ecr.us-east-1.amazonaws.com/webapp:latest
    ports:
    - containerPort: 8000
    resources:
      requests:
        memory: "256Mi"
        cpu: "250m"
      limits:
        memory: "512Mi"
        cpu: "500m"
```

### Deployment

Gestiona replicas de Pods:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp
        image: 123456789.dkr.ecr.us-east-1.amazonaws.com/webapp:latest
        ports:
        - containerPort: 8000
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
```

### Service

Expone pods como red:

```yaml
apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
  selector:
    app: webapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer
```

### ReplicaSet

Mantiene número de pods:

```yaml
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: webapp-replicaset
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  template:
    # ... mismo template que Deployment
```

---

## EKS Node Groups

### Crear Node Group (EC2)

```bash
# Usando eksctl
eksctl create nodegroup \
    --cluster my-cluster \
    --name ng-1 \
    --instance-types t3.medium \
    --desired-capacity 2 \
    --min-size 1 \
    --max-size 4 \
    --ssh-access \
    --ssh-public-key my-key

# Ver Node Groups
aws eks list-nodegroups --cluster my-cluster

# Describe Node Group
aws eks describe-nodegroup \
    --cluster-name my-cluster \
    --nodegroup-name ng-1
```

### Fargate Profile

```bash
# Crear Fargate Profile
aws eks create-fargate-profile \
    --cluster-name my-cluster \
    --pod-execution-role-arn arn:aws:iam::123456789:role/eks-fargate-pod-role \
    --selectors '[{"namespace": "default"}, {"namespace": "webapp", "labels": {"env": "production"}}]' \
    --fargate-profile-name webapp-profile
```

---

## kubectl - Comandos Esenciales

### Conexión a EKS

```bash
# Actualizar kubeconfig
aws eks update-kubeconfig --name my-cluster

# Ver contextos
kubectl config get-contexts

# Usar contexto
kubectl config use-context arn:aws:eks:us-east-1:123456789:cluster/my-cluster

# Ver información del cluster
kubectl cluster-info
```

### Gestión de Pods

```bash
# Listar pods
kubectl get pods

# Listar pods en todos los namespaces
kubectl get pods -A

# Ver detalles del pod
kubectl describe pod my-app-pod

# Ver logs
kubectl logs my-app-pod

# Logs en tiempo real
kubectl logs -f my-app-pod

# Ejecutar comando en pod
kubectl exec -it my-app-pod -- /bin/bash

# Eliminar pod
kubectl delete pod my-app-pod
```

### Deployments

```bash
# Listar deployments
kubectl get deployments

# Aplicar configuración
kubectl apply -f deployment.yaml

# Ver status
kubectl rollout status deployment/webapp-deployment

# Ver replicas
kubectl get rs

# Escalar deployment
kubectl scale deployment webapp-deployment --replicas=5

# Actualizar imagen
kubectl set image deployment/webapp-deployment webapp=my-image:v2

# Rollback
kubectl rollout undo deployment/webapp-deployment
```

### Services

```bash
# Listar services
kubectl get services

# Listar con más detalle
kubectl get svc -o wide

# Ver endpoints
kubectl get endpoints

# Describe service
kubectl describe service webapp-service

# Eliminar service
kubectl delete service webapp-service
```

---

## Service Types

| Type | Descripción | Caso de uso |
|------|-------------|-------------|
| **ClusterIP** | IP interna del cluster | Comunicación interna |
| **NodePort** | Expone en puerto del nodo | Desarrollo |
| **LoadBalancer** | Crea ALB/NLB de AWS | Producción |
| **ExternalName** | CNAME externo | Integración con external services |

---

## Namespaces

```bash
# Listar namespaces
kubectl get namespaces

# Crear namespace
kubectl create namespace webapp

# Ver recursos en namespace
kubectl get pods -n webapp

# Aplicar a namespace específico
kubectl apply -f deployment.yaml -n webapp
```

### Namespace en YAML

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: webapp
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-deployment
  namespace: webapp
```

---

## Ingress (ALB Ingress Controller)

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-ingress
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: webapp-service
            port:
              number: 80
```

### Instalar ALB Ingress Controller

```bash
# Usar Helm o eksctl
eksctl install addon --name aws-load-balancer-controller --cluster my-cluster

# O manualmente con kubectl
kubectl apply -f https://raw.githubusercontent.com/kubernetes-sigs/aws-load-balancer-controller/main/docs/install/2.6.1_full.yaml
```

---

## ConfigMaps y Secrets

### ConfigMap

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  ENV: "production"
  LOG_LEVEL: "info"
---
# Uso en Pod
envFrom:
- configMapRef:
    name: app-config
```

### Secret

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  API_KEY: "your-api-key-here"
  DB_PASSWORD: "your-db-password"
---
# Uso en Pod
envFrom:
- secretRef:
    name: app-secrets
```

---

## RBAC - Roles y ServiceAccounts

### ServiceAccount para Pod

```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: webapp-sa
  namespace: webapp
---
# Pod usa el ServiceAccount
apiVersion: apps/v1
kind: Deployment
spec:
  template:
    spec:
      serviceAccountName: webapp-sa
```

### Role y RoleBinding

```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-reader
  namespace: webapp
rules:
- apiGroups: [""]
  resources: ["pods", "services"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-reader-binding
  namespace: webapp
subjects:
- kind: ServiceAccount
  name: webapp-sa
  namespace: webapp
roleRef:
  kind: Role
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io
```

---

## Horizontal Pod Autoscaler (HPA)

```bash
# Crear HPA
kubectl autoscale deployment webapp-deployment \
    --cpu-percent=70 \
    --min=2 \
    --max=10

# Ver HPA
kubectl get hpa

# Describe HPA
kubectl describe hpa webapp-deployment
```

### HPA en YAML

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: webapp-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: webapp-deployment
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
```

---

## Troubleshooting

| Problema | Causa | Solución |
|----------|-------|----------|
| `ImagePullBackOff` | Imagen no existe o credenciales | Verificar nombre de imagen, ECR permissions |
| `CrashLoopBackOff` | App falla al iniciar | `kubectl logs` y `kubectl describe` |
| `Pending pods` | Recursos insuficientes o no hay nodos | Ver Node Group, scaling |
| `Terminating` stuck | Finalizadores no completan | `kubectl delete --force` |
| `Unauthorized` | RBAC sin permisos | Ver ServiceAccount y RoleBindings |
| `Evicted` | Node sin recursos o pod priority | Reducir recursos o nodes |

---

## Comandos Útiles para Debug

```bash
# Ver eventos del namespace
kubectl get events -n webapp --sort-by='.lastTimestamp'

# Ver uso de recursos
kubectl top pods
kubectl top nodes

# Ver todos los recursos
kubectl get all -n webapp

# Ver nodos
kubectl get nodes -o wide

# Describe nodo
kubectl describe node ip-xxx.ec2.internal
```
